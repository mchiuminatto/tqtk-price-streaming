# deploy

Deployment assets for the tqtk price pipeline.

- Docker Compose stack (services + Redis + PostgreSQL/TimescaleDB + Prometheus + Grafana) — added
  in the platform-components tasks.
- minikube manifests — a later step, per the architecture note.

Bring-up and teardown steps are documented here as the stack lands (task 13.4).
